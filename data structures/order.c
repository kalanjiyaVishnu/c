#include <stdio.h>

int main()
{
    // int i, j, n, m;
    // printf("enter the size n and m\n");
    // scanf("%d %d",&n,&m);

    // printf("enter the elemetsn\n");
    int a[] = {2, 4, 9, 12, 11};
    int b[] = {2, 4, 9, 12};

    // printf("%d",n);
    //     for (int i = 0; i < n - 1; i++)
    //     {
    //         if (a[i] != b[i])
    //         {
    //             printf("%d---> %d", i, a[i]);
    //             return 0;
    //         }
    //     }
    //     printf("%d---> %d", n, a[n-1]);

    // FIND THE MISSING LEAST ELEMETN 1,2,3,4,5 --> 6 ELEMETN IS THE ANSWER

    int x[] = {1, 2, 3, 4, 5};

    int n = sizeof(x) / sizeof(x[0]);
    for (int i = 0; i < n; i++)
    {
        for(int i)
    }
    return 0;
}